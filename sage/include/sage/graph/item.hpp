#ifndef SAGE_ITEM_HPP
#define SAGE_ITEM_HPP

namespace sage {

  namespace item {

    // Scope: {
    //    sage::item::Property -> std::optional<sage::item::Type,
    //    sage::item::Scope>
    // },
    // [ sage::item::Scope, ]
    struct Scope {
      Scope();
      ~Scope();
    };

    enum Type {

    };

    enum Property {

    };

  }  // namespace item

}  // namespace sage
#endif  // !SAGE_ITEM_HPP